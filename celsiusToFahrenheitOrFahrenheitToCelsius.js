const celsiusToFahrenheitOrFahrenheitToCelsius = temp => !/[cf]/i.test(temp[temp.length - 1]) || /[cf]/i.test(temp[0] || temp[0] === ' ') ? new Error('invalid input; the valid format is #C (or #c) or #F (or #f).') : /c/i.test(temp) ? `${(temp.replace(/c/i, '') * (9 / 5) + 32)} °F` : `${(temp.replace(/f/i, '') - 32) / 1.8} °C`;
